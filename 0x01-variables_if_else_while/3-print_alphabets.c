#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 for successful execution
 */
int main(void)
{
int lowerCase = 97;
int upperCase = 65;
while (lowerCase <= 122)
{
	putchar(num);
	num++;
}
while (upperCase <= 90)
{
	putchar(num);
	num++;
}
putchar('\n');
return (0);
}
