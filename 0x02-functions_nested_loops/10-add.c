#include "main.h"

/**
 * add - function name.
 * @a: to be added to b.
 * @b : to be added to a.
 *
 * Description: function that adds two integers and returns the result.
 *
 * Return: sum of a and b.
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
